pipeline{
    agent any
    tools{
        maven 'maven path'
    }
    stages{
        stage('maven'){
            steps{
                echo "maven version enna"
                sh 'mvn --version'
            }
        }
    }
}
