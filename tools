pipeline{
    agent any
    tools{
        maven 'maven 399'
    }
    stages{
        stage('maven'){
            steps{
                echo "maven version enna"
                sh 'mvn --version'
            }
        }
    }
}
